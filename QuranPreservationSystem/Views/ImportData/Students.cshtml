@using QuranPreservationSystem.Domain.Entities
@{
    ViewData["Title"] = "استيراد الطلاب";
    Layout = "_DashboardLayout";
    var tempRecords = ViewBag.TempRecords as IEnumerable<QuranPreservationSystem.Domain.Entities.TempStudentImport>;
}

<!-- Page Header -->
<div class="page-header">
    <div class="page-title">
        <i class="fas fa-user-graduate"></i>
        <h2>استيراد بيانات الطلاب</h2>
    </div>
    <div class="page-actions">
        <a href="@Url.Action("DownloadStudentsTemplate")" class="btn btn-success">
            <i class="fas fa-download"></i> تحميل القالب
        </a>
        <a href="@Url.Action("Index")" class="btn btn-outline">
            <i class="fas fa-arrow-right"></i> العودة
        </a>
    </div>
</div>

<!-- Messages -->
@if (TempData["Success"] != null)
{
    <div class="alert alert-success">
        <i class="fas fa-check-circle"></i> @TempData["Success"]
    </div>
}
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">
        <i class="fas fa-exclamation-circle"></i> @TempData["Error"]
    </div>
}
@if (TempData["Warning"] != null)
{
    <div class="alert alert-warning">
        <i class="fas fa-exclamation-triangle"></i> @TempData["Warning"]
    </div>
}

<!-- Upload Section -->
<div class="card">
    <div class="card-header">
        <i class="fas fa-upload"></i>
        <span>رفع ملف Excel</span>
    </div>
    <div class="card-body">
        <form asp-action="UploadStudents" method="post" enctype="multipart/form-data">
            <div class="upload-area">
                <div class="upload-icon">
                    <i class="fas fa-file-excel"></i>
                </div>
                <h4>اختر ملف Excel للرفع</h4>
                <p>قم بتحميل القالب أولاً، املأه ببيانات الطلاب، ثم ارفعه هنا</p>
                
                <input type="file" 
                       name="excelFile" 
                       id="excelFile" 
                       accept=".xlsx,.xls" 
                       class="file-input" 
                       required />
                
                <label for="excelFile" class="file-label">
                    <i class="fas fa-cloud-upload-alt"></i>
                    <span class="file-text">اختر ملف Excel</span>
                </label>

                <div class="upload-actions">
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-upload"></i> رفع ومعالجة
                    </button>
                    <a href="@Url.Action("DownloadStudentsTemplate")" class="btn btn-success">
                        <i class="fas fa-download"></i> تحميل القالب
                    </a>
                </div>
            </div>
        </form>
    </div>
</div>

<!-- Template Info -->
<div class="card" style="margin-top: 20px;">
    <div class="card-header" style="background: linear-gradient(135deg, #1976d2 0%, #1565c0 100%);">
        <i class="fas fa-info-circle"></i>
        <span>معلومات القالب - 12 عمود (مطابق لـ Student)</span>
    </div>
    <div class="card-body">
        <div class="template-info">
            <div class="info-section">
                <h4><i class="fas fa-star text-danger"></i> الحقول المطلوبة (6 حقول):</h4>
                <div class="fields-grid">
                    <div class="field-item required">
                        <i class="fas fa-check-circle"></i>
                        <span><strong>1. الاسم الأول</strong></span>
                    </div>
                    <div class="field-item required">
                        <i class="fas fa-check-circle"></i>
                        <span><strong>2. اسم العائلة</strong></span>
                    </div>
                    <div class="field-item required">
                        <i class="fas fa-check-circle"></i>
                        <span><strong>3. رقم الهاتف</strong></span>
                    </div>
                    <div class="field-item required">
                        <i class="fas fa-check-circle"></i>
                        <span><strong>4. المركز</strong> (موجود في النظام)</span>
                    </div>
                    <div class="field-item required">
                        <i class="fas fa-check-circle"></i>
                        <span><strong>5. تاريخ الميلاد</strong> (dd/mm/yyyy)</span>
                    </div>
                    <div class="field-item required">
                        <i class="fas fa-check-circle"></i>
                        <span><strong>6. الجنس</strong> (ذكر/أنثى)</span>
                    </div>
                </div>
            </div>

            <div class="info-section">
                <h4><i class="fas fa-list-check text-success"></i> الحقول الاختيارية (6 حقول):</h4>
                <div class="fields-grid">
                    <div class="field-item optional">
                        <i class="fas fa-circle"></i>
                        <span>7. البريد الإلكتروني</span>
                    </div>
                    <div class="field-item optional">
                        <i class="fas fa-circle"></i>
                        <span>8. العنوان</span>
                    </div>
                    <div class="field-item optional">
                        <i class="fas fa-circle"></i>
                        <span>9. تاريخ التسجيل (dd/mm/yyyy)</span>
                    </div>
                    <div class="field-item optional">
                        <i class="fas fa-circle"></i>
                        <span>10. المستوى التعليمي</span>
                    </div>
                    <div class="field-item optional">
                        <i class="fas fa-circle"></i>
                        <span>11. ملاحظات</span>
                    </div>
                    <div class="field-item optional">
                        <i class="fas fa-circle"></i>
                        <span>12. نشط (نعم/لا)</span>
                    </div>
                </div>
            </div>

            <div class="info-section">
                <h4><i class="fas fa-exclamation-triangle text-warning"></i> ملاحظات هامة:</h4>
                <ul class="important-notes">
                    <li>📅 <strong>التواريخ:</strong> استخدم صيغة dd/mm/yyyy (مثال: 15/03/2010)</li>
                    <li>👤 <strong>الجنس:</strong> اكتب "ذكر" أو "أنثى" فقط</li>
                    <li>✔️ <strong>نشط:</strong> اكتب "نعم" أو "لا"</li>
                    <li>🏢 <strong>المركز:</strong> تأكد من وجود المركز في النظام قبل الرفع</li>
                    <li>💡 <strong>الحقول الاختيارية:</strong> 6 حقول يمكن تركها فارغة</li>
                </ul>
            </div>

            <div class="template-stats">
                <div class="stat-box required-box">
                    <i class="fas fa-star"></i>
                    <div class="stat-info">
                        <h5>6</h5>
                        <p>حقول مطلوبة</p>
                    </div>
                </div>
                <div class="stat-box optional-box">
                    <i class="fas fa-list"></i>
                    <div class="stat-info">
                        <h5>6</h5>
                        <p>حقول اختيارية</p>
                    </div>
                </div>
                <div class="stat-box total-box">
                    <i class="fas fa-columns"></i>
                    <div class="stat-info">
                        <h5>12</h5>
                        <p>إجمالي الأعمدة</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Recent Imports -->
@if (tempRecords != null && tempRecords.Any())
{
    <div class="card" style="margin-top: 20px;">
        <div class="card-header">
            <i class="fas fa-history"></i>
            <span>آخر عمليات الاستيراد (50 سجل)</span>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="data-table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>اسم الطالب</th>
                            <th>الهاتف</th>
                            <th>المركز</th>
                            <th>المستوى التعليمي</th>
                            <th>الحالة</th>
                            <th>تاريخ الرفع</th>
                            <th>رفع بواسطة</th>
                            <th>رسالة الخطأ</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            int index = 1;
                        }
                        @foreach (var record in tempRecords)
                        {
                            <tr>
                                <td>@index</td>
                                <td><strong>@record.FirstName @record.LastName</strong></td>
                                <td>@record.PhoneNumber</td>
                                <td>@record.CenterName</td>
                                <td>@(record.EducationLevel ?? "-")</td>
                                <td>
                                    @switch (record.Status)
                                    {
                                        case ImportStatus.Pending:
                                            <span class="badge badge-warning">في الانتظار</span>
                                            break;
                                        case ImportStatus.Processing:
                                            <span class="badge badge-info">قيد المعالجة</span>
                                            break;
                                        case ImportStatus.Completed:
                                            <span class="badge badge-success">تم بنجاح</span>
                                            break;
                                        case ImportStatus.Failed:
                                            <span class="badge badge-danger">فشل</span>
                                            break;
                                        case ImportStatus.Duplicate:
                                            <span class="badge badge-secondary">مكرر</span>
                                            break;
                                    }
                                </td>
                                <td>@record.UploadedDate.ToString("dd/MM/yyyy HH:mm")</td>
                                <td>@(record.UploadedBy ?? "-")</td>
                                <td>
                                    @if (!string.IsNullOrEmpty(record.ErrorMessage))
                                    {
                                        <span class="text-danger" title="@record.ErrorMessage">
                                            @(record.ErrorMessage.Length > 50 ? record.ErrorMessage.Substring(0, 50) + "..." : record.ErrorMessage)
                                        </span>
                                    }
                                    else
                                    {
                                        <text>-</text>
                                    }
                                </td>
                            </tr>
                            index++;
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}

@section Styles {
    <link rel="stylesheet" href="~/css/dashboard.css" />
    <style>
        .upload-area {
            text-align: center;
            padding: 40px;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            border-radius: 12px;
            border: 2px dashed var(--border-color);
        }

        .upload-icon {
            font-size: 4rem;
            color: var(--primary-color);
            margin-bottom: 20px;
            animation: pulse 2s infinite;
        }

        @@keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .upload-area h4 {
            color: var(--primary-dark);
            margin-bottom: 10px;
            font-weight: 700;
        }

        .upload-area p {
            color: #666;
            margin-bottom: 30px;
        }

        .file-input {
            display: none;
        }

        .file-label {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 15px 30px;
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
            color: white;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-weight: 600;
            margin-bottom: 20px;
        }

        .file-label:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(25, 118, 210, 0.3);
        }

        .upload-actions {
            display: flex;
            gap: 15px;
            justify-content: center;
        }

        .template-info {
            padding: 20px;
        }

        .info-section {
            margin-bottom: 30px;
        }

        .info-section h4 {
            color: var(--primary-dark);
            margin-bottom: 15px;
            font-size: 1.2rem;
            display: flex;
            align-items: center;
            gap: 10px;
            padding-bottom: 10px;
            border-bottom: 2px solid #e0e0e0;
        }

        .fields-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 12px;
            margin-top: 15px;
        }

        .field-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 10px 15px;
            border-radius: 8px;
            transition: all 0.2s;
        }

        .field-item.required {
            background: #ffebee;
            border-right: 4px solid #d32f2f;
        }

        .field-item.optional {
            background: #e8f5e9;
            border-right: 4px solid #43a047;
        }

        .field-item i {
            font-size: 0.9rem;
        }

        .field-item.required i {
            color: #d32f2f;
        }

        .field-item.optional i {
            color: #43a047;
        }

        .important-notes {
            margin: 15px 0;
            padding: 0;
            list-style: none;
        }

        .important-notes li {
            padding: 12px 15px;
            margin-bottom: 8px;
            background: #fff3e0;
            border-right: 4px solid #ff9800;
            border-radius: 6px;
            line-height: 1.6;
        }

        .template-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 30px;
        }

        .stat-box {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 20px;
            border-radius: 10px;
            color: white;
        }

        .required-box {
            background: linear-gradient(135deg, #e53935 0%, #c62828 100%);
        }

        .optional-box {
            background: linear-gradient(135deg, #43a047 0%, #2e7d32 100%);
        }

        .total-box {
            background: linear-gradient(135deg, #1976d2 0%, #1565c0 100%);
        }

        .stat-box i {
            font-size: 2.5rem;
            opacity: 0.9;
        }

        .stat-info h5 {
            font-size: 2rem;
            font-weight: 700;
            margin: 0;
        }

        .stat-info p {
            margin: 0;
            opacity: 0.9;
        }

        .alert {
            padding: 15px 20px;
            border-radius: 8px;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .alert-success {
            background: #e8f5e9;
            color: #2e7d32;
            border-right: 4px solid #43a047;
        }

        .alert-danger {
            background: #ffebee;
            color: #c62828;
            border-right: 4px solid #e53935;
        }

        .alert-warning {
            background: #fff3e0;
            color: #e65100;
            border-right: 4px solid #ff9800;
        }
    </style>
}

@section Scripts {
    <script>
        $(document).ready(function() {
            $('#excelFile').on('change', function() {
                var fileName = $(this).val().split('\\').pop();
                if (fileName) {
                    $('.file-text').text(fileName);
                } else {
                    $('.file-text').text('اختر ملف Excel');
                }
            });
        });
    </script>
}

